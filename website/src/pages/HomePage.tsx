// Full implementation lands in issue #9. Stub renders the hero heading so
// routing works and the h1 is present.
export function HomePage() {
  return (
    <section style={{ maxWidth: '1200px', margin: '0 auto', padding: 'clamp(40px,6vw,86px) var(--space-4)' }}>
      <h1>Explore Mount Mulanje. Discover Southern Malawi.</h1>
    </section>
  );
}
